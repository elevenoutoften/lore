# Task: flow_000876 — Add test driving the real vector indexing-failure raise path to the drift gauge

## Context

The AC clause "a forced indexing failure surfaces in the drift gauge rather than silently vanishing" is met IN CODE (route_utils.py:241-247 `index_vectors_for_page` wraps `upsert_page_chunks` in try/except, calls `record_pending_reindex` on failure, then re-raises). But no COMMITTED test drives the REAL raise path. The existing tests either inject the pending marker manually (`test_vector_index_reconciliation_rebuilds_drift_scope_and_clears_pending`, line 96) or use `FailingEmbeddings` whose failure is swallowed INSIDE `upsert_page_chunks` (vector_store.py:199-201, dense-only) so `record_pending_reindex` is never reached.

## What to Implement

### 1. Add test that forces upsert_page_chunks itself to raise
File: `tests/test_rag.py`. Add a test `test_indexing_failure_surfaces_in_drift_gauge` that:

1. Creates a `LoreRepository` at `tmp_path / "pages"`, calls `repo.ensure_root()`, and upserts a page (e.g. `concepts/drift-test` with some content).
2. Creates a `VectorStore` at `tmp_path / "vectors.db"`.
3. Monkeypatches `vector_store.upsert_page_chunks` to raise an exception (e.g. `RuntimeError("simulated indexing failure")`). Use `monkeypatch` fixture or direct method replacement.
4. Calls `index_vectors_for_page(vector_store, page)` and asserts it re-raises the exception (use `pytest.raises`).
5. After the raise, asserts:
   - `vector_store.pending_reindex_count() >= 1` (the pending marker was recorded)
   - `vector_index_stats(repo, store)["page_drift"] >= 1` (the drift gauge reports it)
6. Verifies the test FAILS if the try/except+record block is removed from `index_vectors_for_page` (i.e. the test asserts both the re-raise AND the pending marker, so removing the record_pending_reindex call would fail the `pending_reindex_count >= 1` assertion).

Follow the existing test patterns in `test_rag.py`. Import `index_vectors_for_page` from `lore_app.route_utils` and `vector_index_stats` from the same module (already imported at line 16). Import `LoreRepository` and `VectorStore` (already imported in the file).

## Files NOT to Modify
- `lore_app/route_utils.py` — the code is correct, only add the test
- Any production code — this is a test-only task

## Acceptance Criteria
1. New test forces `upsert_page_chunks` to raise (via monkeypatch)
2. Asserts `pending_reindex_count() >= 1`
3. Asserts `vector_index_stats` reports the drift (`page_drift >= 1`)
4. Asserts the exception re-raises
5. Removing the try/except-and-record block from `index_vectors_for_page` makes the test fail
6. All existing tests still pass

## Test Command
```bash
cd /home/nyx/.hermes/projects/lore && LORE_CONTENT_DIR=/tmp/test-pages LORE_SEARCH_DB=/tmp/test-search.db LORE_LEDGER_DB=/tmp/test-ledger.db LORE_VECTOR_DB=/tmp/test-vec.db . .venv/bin/activate && python -m pytest tests/test_rag.py -v
```

## Commit Message
```
test: add test for real vector indexing-failure raise path to drift gauge (flow_000876)
```