name: lore
version: 2.1.0
description: "Lore knowledge backend — structured memory with page search/read and the durable capture/recall/ack loop. Uses lore_sdk for API calls."
hooks:
  - on_memory_write
  - on_pre_compress
# Dependencies are resolved by the provider via LORE_SDK_PATH or the bundled SDK path.
